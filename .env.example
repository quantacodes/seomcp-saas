# SEO MCP SaaS — Environment Variables
# Copy this to .env and fill in your values

# ── Required ──
NODE_ENV=production
PORT=3456
HOST=0.0.0.0                           # Bind address (0.0.0.0 for containers)
JWT_SECRET=                            # Generate: openssl rand -hex 32
TOKEN_ENCRYPTION_KEY=                  # Generate: openssl rand -hex 32

# ── Admin ──
ADMIN_SECRET=                          # Generate: openssl rand -hex 32 (for /api/admin/* endpoints)
TRUSTED_PROXY=true                     # Set true behind reverse proxy (Fly.io, Cloudflare) for IP rate limiting

# ── SEO MCP Binary ──
SEO_MCP_BINARY=/app/seo-mcp-server     # Path to the Rust binary

# ── Database ──
DATABASE_PATH=/app/data/seo-mcp-saas.db

# ── Base URL ──
BASE_URL=https://seomcp.dev

# ── Google OAuth (for user GSC/GA4 access) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://seomcp.dev/api/auth/google/callback

# ── Email Verification (Resend) ──
RESEND_API_KEY=                        # From resend.com dashboard (free: 100/day)
RESEND_FROM_EMAIL=verify@seomcp.dev    # Must be verified domain in Resend

# ── Lemon Squeezy Billing ──
LEMONSQUEEZY_API_KEY=                  # From LS Settings → API
LEMONSQUEEZY_STORE_ID=                 # Your store ID
LEMONSQUEEZY_WEBHOOK_SECRET=           # Webhook signing secret
LEMONSQUEEZY_PRO_VARIANT_ID=           # Pro plan variant ID
LEMONSQUEEZY_AGENCY_VARIANT_ID=        # Agency plan variant ID
